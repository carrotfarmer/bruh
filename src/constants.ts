import { ConfigColorMode } from "@chakra-ui/react";

// this file contains all the constants/magic numbers used in the app
export enum Speeds {
  INSTANT_DETH_SPEED = 1,
  THICC_SPEED = 2.5,
  SLOWWW_SPEED = 5,
  SLOW_SPEED = 7.5,
  NORMAL_SPEED = 10,
  RACECAR_SPEED = 15,
  ROCKET_SPEED = 20,
}

export const DEFAULT_THEME: ConfigColorMode = "dark";
